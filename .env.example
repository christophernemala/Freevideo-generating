# Existing video-use transcription
ELEVENLABS_API_KEY=

# Hosted generation. `mock` runs without a paid inference API.
VIDEO_PROVIDER=mock

# Optional custom/self-hosted GPU generation endpoint.
# Keep credentials server-side; never prefix these with NEXT_PUBLIC_.
VIDEO_PROVIDER_BASE_URL=
VIDEO_PROVIDER_API_KEY=
